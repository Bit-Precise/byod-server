apiVersion: v1
kind: Service
metadata:
  name: {{ include "byod-middleware.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "byod-middleware.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8787
